# TODO

- [ ] Refactor the git log checking. We can use `git log <branch> -1` to check last commit relative date
- [ ] Refactor git log fetching. By using `git log <branch>` we can get the logs from that branch without checking out into it
